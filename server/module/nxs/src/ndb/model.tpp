namespace ndb
{
    /*
    template<class Engine>
    const typename setup<Engine>::expression_type& model_base::native() const
    {
        return static_cast<const model<Engine>&>(*this).native();
    }
    */

} // ndb
